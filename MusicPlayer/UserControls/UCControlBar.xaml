<UserControl x:Class="MusicPlayer.UserControl.ControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayer.UserControl"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="1440">
    <Grid>
        <Border CornerRadius="20,20,0,0"  Background="{StaticResource PrimaryMid}">
            <DockPanel>
                <ToolBarTray DockPanel.Dock="Right" Background="Transparent" Margin="0,0,-20,0">
                    <ToolBar Background="Transparent" Margin="0,-10,0,0">
                        <Button  Content="{materialDesign:PackIcon Kind=Minimize}" />
                        <Separator Width="10" Background="Transparent"/>
                        <Button  Content="{materialDesign:PackIcon Kind=Close}" />
                    </ToolBar>
                </ToolBarTray>
                <TextBox AcceptsReturn="True" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
